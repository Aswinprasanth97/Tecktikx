Small pill label. Neutral `pill` for category tags; `coral` (uppercase, tracked) for NEW / BETA.

```jsx
<Badge>Research</Badge>
<Badge variant="coral">New</Badge>
<Badge variant="amber">Beta</Badge>
```
