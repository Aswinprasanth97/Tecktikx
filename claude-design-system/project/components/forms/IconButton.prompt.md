36px circular icon button for carousel arrows, share, and view-more. Pass an SVG/glyph as children.

```jsx
<IconButton ariaLabel="Next"><ArrowRightSvg /></IconButton>
<IconButton variant="on-dark" ariaLabel="Share">↗</IconButton>
```

Variants: `default` (cream + hairline) and `on-dark`.
